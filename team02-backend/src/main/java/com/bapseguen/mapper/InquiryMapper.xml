<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="inquiry">
	<!-- 고객센터 문의 목록 조회  -->
	<!-- <select id="selectAll" resultType="InquiryDetailDTO">
	<![CDATA[
		SELECT*FROM(
			SELECT ROWNUM AS RNUM,SUBQUERY.*
			FROM(
			SELECT MEMBER_NUMBER,MEMBER_ID,POST_NUMBER,POST_Title,POST_CREATED_DATE,POST_VIEW_COUNT,INQUIRY_STATUS
			FROM TBL_INQUIRY
			ON M.MEMBER_NUMBER=P.MEMBER_NUMBER
			ORDER BY P.POST_CREATED_DATE DESC
		)SUBQUERY
	)WHERE RNUM BETWEEN #{startRow} AND #{endRow}
	]]>
	</select> -->

	<!--조회수 쿼리  -->
	<update id="updateReadCount" parameterType="int">
		UPDATE TBL_INQUIRY
		SET POST_VIEW_COUNT=POST_VIEW_COUNT+1
		WHERE POST_NUMBER=#{postNumber}
	</update>


	<!-- 고객센터 문의 상세(단건조회) -->
<!-- 	<select id="select" parameterType="int" resultType="InquiryDetailDTO">
		
		
		<![CDATA[
			SELECT M.MEMBER_NUMBER,M.MEMBER_ID, P.POST_NUMBER,P.POST_TITLE,P.POST_CONTENT,P.POST_DATE,P.POST_VIEW_COUNT
			FROM TBL_MEMBER M JOIN TBL_POST P
			ON P.MEMBER_NUMBER=M.MEMBERNUMBER
			WHERE P.POST_NUMBER= #{postNumber}
		]]>
		
		
	</select> -->



</mapper>