<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="postImage">
	<!-- 파일 추가 -->
	<insert id="insert" parameterType="PostImageDTO">
		INSERT INTO TBL_POST_IMAGE(
		POST_IMAGE_SYSTEM_NAME AS postImageSystemName,
		POST_IMAGE_ORIGINAL_NAME AS postImageOriginalName,
		POST_NUMBER AS postNumber)
		VALUES (#{POST_IMAGE_SYSTEM_NAME}, #{POST_IMAGE_ORIGINAL_NAME}, #{POST_NUMBER})	
	</insert>
	
	<!-- 파일 조회 -->
	<select id="select" parameterType="int" resultType="PostImageDTO">
		SELECT POST_IMAGE_SYSTEM_NAME AS postImageSystemName,
		POST_IMAGE_ORIGINAL_NAME AS postImageOriginalName,
		POST_NUMBER AS postNumber
		FROM TBL_POST_IMAGE
		WHERE POST_NUMBER = #{postNumber}
	</select>
	
	<!-- 파일 삭제 -->
	<delete id="delete" parameterType="int">
		DELETE FROM TBL_POST_IMAGE
		WHERE POST_NUMBER = #{postNumber}
	</delete>

</mapper>