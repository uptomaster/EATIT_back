<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
   <!-- 회원가입 쿼리작성 -->
   <insert id="generaljoin" parameterType="MemberDTO">
		insert into tbl_member(member_number, member_id, member_password, member_name, member_birthdate, member_phone_number)
		values (seq_member.nextval, #{memberId}, #{memberPassword}, #{memberName}, #{memberBirthdate}, #{memberPhoneNumber})
   </insert>
   
	<!-- 로그인 쿼리작성 -->
	<select id="login" parameterType="MemberDTO" resultType="int">
		select member_number from tbl_member
		where member_id = #{memberId} and member_password = #{memberPassowrd}
	</select>
</mapper>


<!-- 	private int memberNumber;
	private String memberType;
	private String memberId;
	private String memberPassword;
	private String memberName;
	private String memberBirthdate;
	private String memberPhoneNumber;
	private String memberJoinDate;
	private String memberUpdatedDate;
	private int memberPaymentAmount;
	private int memberPoint;
	private String memberTreeGrade; -->