<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="seller">
<!-- 내 정보 조회 -->
	<select id="selectMyinfo" resultType="SellerInfoDTO">
	<![CDATA[
		SELECT MEMBER_NUMBER,MEMBER_ID,MEMBER_PASSWORD,MEMBER_NAME,MEMBER_PHONE_NUMBER,MEMBER_BIRTHDATE,
		FROM(
		SELECT M.MEMBER_NUMBER,M.MEMBER_ID,M.MEMBER_PASSWORD,G.MEMBER_NAME,G.MEMBER_PHONE_NUMBER,G.MEMBER_BIRTHDATE
		FROM TBL_MEMBER M JOIN TBL_GENERAL_MEMBER G
		ON M.MEMBER_NUMBER=G.MEMBER_NUMBER
		)SUBQUERY
	]]>
	</select>
	
	<!-- 내 정보 수정 -->
	<update id="updateMyinfo" parameterType="SellerInfoDTO">
		<![CDATA[
		UPDATE MEMBER_NUMBER,MEMBER_ID,MEMBER_PASSWORD,MEMBER_NAME,MEMBER_PHONE_NUMBER,MEMBER_BIRTHDATE,
		SET(
		UPDATE M.MEMBER_NUMBER,M.MEMBER_ID,M.MEMBER_PASSWORD,G.MEMBER_NAME,G.MEMBER_PHONE_NUMBER,G.MEMBER_BIRTHDATE
		SET TBL_MEMBER M JOIN TBL_GENERAL_MEMBER G
		ON M.MEMBER_NUMBER=G.MEMBER_NUMBER
		)SUBQUERY
	]]>
	</update>
	<!-- 전화번호 중복 여부 -->
	
	
	<!-- 쿼리문에 문제가 업다면 일반 사용자의 마이페이지의 맵퍼를 사용하고 싶음 -->
	<!-- 내 게시글 목록 조회 -->
	<!-- 내 댓글 목록 조회 -->
	<!-- 내 리뷰 목록 조회 -->
	<!-- 구매한 음식 내역 조회 -->
	<!-- 구매한 재료 내역 조회 -->
	<!-- 리뷰 작성 -->
	<!-- 리뷰 작성 여부 확인 -->
</mapper>